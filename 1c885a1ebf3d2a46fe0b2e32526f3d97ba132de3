Tool-native error handling.

Got tired of `pocket`. Still good for when you want to `case` on errors as
with `op`, but for running `gh` and `git` and checking every error, it starts
to get to blocky, too if/else.
